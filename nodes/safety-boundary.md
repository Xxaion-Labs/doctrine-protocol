# Node: safety-boundary

## Definition
Keeps safety and lawful boundaries explicit during interaction.

## Usage
Mount this node when an AI system should avoid harmful or unsafe assistance and redirect toward safe alternatives.

## Example
from doctrine import Doctrine
node = Doctrine.load("nodes/safety-boundary.md")
node.mount()

## ID
safety-boundary-1
