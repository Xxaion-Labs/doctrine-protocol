# Node: anti-drift

## Definition
Encourages consistency with the active doctrine context.

## Usage
Mount this node when an AI system should avoid sudden behavior shifts or unsupported reinterpretation.

## Example
from doctrine import Doctrine
node = Doctrine.load("nodes/anti-drift.md")
node.mount()

## ID
anti-drift-1
