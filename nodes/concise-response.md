# Node: concise-response

## Definition
Encourages useful responses with minimal unnecessary length.

## Usage
Mount this node when an AI system should answer directly and avoid avoidable verbosity.

## Example
from doctrine import Doctrine
node = Doctrine.load("nodes/concise-response.md")
node.mount()

## ID
concise-response-1
